//
// Created by qiaoyubo on 2021/8/31.
//

#ifndef CPPTEST_TLPI_HDR_H
#define CPPTEST_TLPI_HDR_H
#include <sys/types.h>
#include <stdio.h>
#include <stdlib.h>

#include <unistd.h>
#include <errno.h>
#include <string.h>
//#include "get_num.h"
#include "error_functions.h"

typedef enum {TRUE, FALSE} Boolean;
#define min(m, n) ((m) < (n) ? (m) : (n))
#define max(m, n) ((m) > (n) ? (m) : (n))
#endif //CPPTEST_TLPI_HDR_H
